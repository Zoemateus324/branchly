// Stub kept for any residual imports — Clerk has been removed.
export const clerkAppearance = {} as const;
export function ClerkFrame({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
